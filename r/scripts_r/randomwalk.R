RandomWalk <- function(N) {
  x <- rnorm(N)
  y <- c(abs(x))
  cat(y)
  # for (i in 1:N) {
  #   R[i] <- y[i] + y[i + 1]
  #   Rmod <- sqrt((y[i] + y[i + 1])^2)
  # }
  # cat(R)
  return(0)
}